<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ElderWatch" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ElderWatch" parent="Base.Theme.ElderWatch" />

    <style name="Theme.ElderWatch.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomThemeBG" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/bg</item>
        <item name="colorSecondary">@color/medium_bg</item>
        <item name="colorAccent">@color/dark_bg</item>
    </style>

    <style name="CustomThemeBGWithAction" parent="Theme.Material3.DayNight">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="colorPrimary">@color/bg</item>
        <item name="colorSecondary">@color/medium_bg</item>
        <item name="colorAccent">@color/dark_bg</item>
    </style>

    <style name="Theme.ElderWatch.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ElderWatch.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>